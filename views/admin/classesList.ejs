<%- include('../includes/head.ejs') %> 
    <link rel="stylesheet" href="/css/login.css">
    </head>
    <body>
    <%- include('../includes/nav.ejs') %>
        <div class="center">
            <% if(classesExist) { %>
                <table class="table">
                    <tr>
                        <th>ردیف</th>
                        <th>شماره کلاس</th>
                    </tr>
                    <% for(let i=0;i<classes.length;i++) { %>
                        <tr>
                            <td><%= i+1 %></td>
                            <td><a href="/admin/class/<%= classes[i].id %>"><%= classes[i].number %> </a></td>
                        </tr>
                    <% } %>  
                </table>
            <% } %>  
        </div>
<%- include('../includes/footer.ejs') %> 